#!/bin/bash

# Get the Python file name from the environment variable
py_file="$PYFILE"

# Check if the file exists
if [[ ! -f "$py_file" ]]; then
    echo "Python file $py_file does not exist."
    exit 1
fi

# Run the Python script
python "$py_file"

